{
  "name": "@react-spring/types",
  "version": "9.0.0-rc.6",
  "description": "Internal package with TypeScript stuff",
  "scripts": {
    "build": "shx mkdir dist && shx cp $(ls --ignore=node_modules --ignore=dist) dist/"
  },
  "dependencies": {
    "@alloc/types": "^1.2.1"
  }
}
